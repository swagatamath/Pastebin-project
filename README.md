# Pastebin-project
Pastebin-Lite is a lightweight web application that allows users to create and share text pastes via a unique URL. Each paste can optionally expire after a set time (TTL) or after a limited number of views. Once a constraint is reached, the paste becomes unavailable.
